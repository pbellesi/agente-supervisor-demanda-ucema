# ==============================================================================
# CONFIGURACIÓN DE ENTORNO: AGENTE SUPERVISOR DE DEMANDA (MBA UCEMA)
# ==============================================================================
# Clave de API de Google Gemini (requerida únicamente para ejecución en modo real).
# Se recomienda operar bajo Free Tier (Costo incremental USD 0).
# No se requiere ninguna clave para ejecutar en modo simulado (--dry-run) ni para tests.
GEMINI_API_KEY=tu_api_key_de_google_ai_studio_aqui

